import React, {useState} from 'react';

export default () => {
    // create state for tracking form input

    // handle submit -- eventually send post req to DB 

    return(
        <>
            register form
        </>
    );
}